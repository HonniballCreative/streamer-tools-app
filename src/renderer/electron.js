const ipcRenderer = window.electron.ipcRenderer;
const loadPreferences = window.electron.loadPreferences;
const loadBlogPosts = window.electron.loadBlogPosts;
const loadUpdatePost = window.electron.loadUpdatePost;
const appVersion = window.electron.appVersion;

export {
  ipcRenderer,
  loadPreferences,
  loadBlogPosts,
  loadUpdatePost,
  appVersion,
}
